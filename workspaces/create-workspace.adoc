---
sidebar: sidebar
permalink: workspaces/create-workspace.html
keywords: aide, workspaces, create workspace, ai data engine
summary: "After you create a cluster, you can create workspaces that allow you to segment data, control data access, and exclude data so as not to expose it to AIDE."
---

= Set up a workspace in AI Data Engine
:icons: font
:imagesdir: ../media/

[.lead]
After you set up a cluster, you can set up a workspace. Workspaces allow you to segment data on the cluster, control data access for individuals, and exclude data that should not be exposed to the AI Data Engine (AIDE).

If you are administering storage, you'll use System Manager to create and manage workspaces, and add data collections to support AI workloads. 

Strategies for workspace creation vary by organization. You might create workspaces based on teams, projects, data sensitivity levels, or other criteria relevant. For example, if you work in healthcare, you might segment clinical data into a workspace but leave out data pertaining to IT, legal, or other departments.

.About this task
Workspace creation is subject to system processing limits (typically up to 15 GB per day per cluster). If you create multiple workspaces in parallel or in quick succession, each workspace might take longer to process, and you might experience significant delays. 

You can monitor the status of workspace creation from the Workspaces inventory page. For best results, avoid creating many workspaces at once if you need immediate access to these features.
//CONTAP-612354

Data collections and Retrieval-Augmented Generation (RAG) search capabilities become available only after the workspace creation process is fully complete.

.Before you begin

* You need _storage administrator_ privileges to create workspaces and associate data collections.
* You've determined the remote and local data sources you intend to use with the workspace and with AI Data Engine.
* You've link:workspaces/prepare-data-for-aide.html[created at least one data container] that can be used by the workspace, such as a local volume or a volume from a peered cluster.
+
IMPORTANT: You want to add a volume to a workspace that won't be deleted for the expected lifetime of that workspace. If you delete a volume once it is part of a workspace, the workspace will enter a failed state. Be confident in the longer term viability of the volume you are using prior to establishing a workspace.

//aidedoc-4

*Ensure NFS is enabled on the volume but that the volume is not CIFS enabled. Workspaces only support volumes with NFS. Volumes with CIFS (SMB) are not supported with workspaces.

// CONTAP-517543
//Should more clusters be added/peered?

== Create a workspace

Create a workspace and associate data containers that contain the data you want to use with AI Data Engine.

.Steps
. In System Manager, navigate to *Data Engine > Workspaces*.
. Select *Add*.
. In the *Add Workspace* dialog, select at least one available data container to associate with the workspace.
. Configure link:prepare-data-for-aide.html[peered clusters] so that the data from those clusters can be accessed within the workspace
. If you'd like to configure user access to the workspace, you can do that now or link:assign-users-to-workspace.html[wait until after the workspace is created].
. Configure a refresh interval for how often the workspace synchronizes with the associated data containers to capture new or updated data (for example, six hours).
+
TIP: Choose an interval that balances data freshness with system performance. If you add a data container to multiple workspaces, the system will automatically use the most aggressive (shortest) interval. To learn more, see documentation about <<Workspace refreshes and versioning,workspaces refreshes and versioning>>.

. Select *Continue*.
. In the *Finalize workspace* dialog, enter a workspace name and description.
. Select *Add* to create the workspace.

.Result
The workspace creation process takes several minutes to hours to complete, depending on the associated dataset and its file count, file size, and other factors.

Metadata will be extracted automatically for all data sources and stored in a metadata catalog that users can use to locate the files they need for their projects. After you assign users to the workspace, they can access and interact with the workspace-affiliated data collections and resources for their AI workloads.

The new workspace will show up in the Workspaces page in `Creating` state until the process is complete and the state changes to `ready`.

//.. (Optional) Choose guardrails from the dropdown. If a guardrail is disabled or in test mode, review any warning messages.

== Review workspace details

After workspace creation, review the workspace details.

.Steps
. Review workspace details, including total size, percentage of cluster capacity used, and the date of most recent workspace refresh.
. Select the workspace name to open the details page.
. In the Overview tab, view workspace details that include associated data containers, users, and activity.

== Workspace refreshes and versioning

Each workspace refresh creates an immutable version that captures the current state of all files and objects in the workspace. Versions include complete metadata, references to snapshots used during extraction, and a job ID for traceability. This supports data lineage, reproducibility, and auditing for AI/ML workloads.

Refreshes occur either on the schedule you configure (such as every six hours) or when you trigger them manually. The minimum supported refresh interval is one hour; the maximum is one year. If a data container is included in multiple workspaces, the most frequent, shortest duration refresh interval is used for scheduling metadata extraction.
//add link to manual refresh instructions

By default, the system retains previous, current, and next (in-progress) versions. Older versions are retained according to your organization's policy and can be purged as needed.
//add link to workspace deletion and volume consequences

You can list all versions of a workspace and view differences between versions to identify which files or objects were added, modified, or deleted. This allows you to track changes over time and understand the evolution of your workspace data.
//Add API table or a link to that table as part of management


//.Related information
//Link to workspace management

