---
sidebar: sidebar
permalink: policies/create-policy.html
keywords: create policy, save query, policy, filters, actions, automation
summary: Save investigation filter criteria as a named policy so you can track the same data population over time and get notified when conditions change.
---
= Create a policy in AI Data Engine
:icons: font
:imagesdir: ../media/

[.lead]
Create a policy when you want to monitor a recurring data condition, such as files going cold or growing beyond a size threshold, without repeating the same investigation manually. For example, a data governance manager can save a "large stale files" policy to track that population across scan cycles and receive an email update when the impacted file count changes.

== About policies

Policies save the filter criteria you define in Investigation as a reusable rule. You can run the policy again at any time to re-examine the same data population, or configure it to automatically take an action, such as tagging, archiving, or deleting, when files meet the policy criteria.

// RESEARCH NEEDED: Confirm the policy execution model for V1. Are policies triggered on a schedule, on demand only, or on a continuous/near-real-time basis as the metadata catalog is updated? This significantly affects how policy behavior should be described.

== Create a policy from Investigation

.Before you begin
* You must have a role that permits creating policies. See link:../administer/manage-users-roles.html[Manage users and roles].

.Steps
. In NetApp Console, navigate to *Governance* > *AI Data Engine* > *Investigation*.
. Apply one or more filters that identify the files you want the policy to target.
+
For example: *File Type* = .vmdk AND *Last Accessed Time* > 365 days.
+
Review the results table to confirm the filter set captures the intended data population.

. In the filter action bar, click *+ Create policy*.
+
// RESEARCH NEEDED: Confirm the exact button label.

. Enter a *Policy name*.

. Optionally, enter a *Description* (up to 500 characters).

. The filter criteria are captured as a query expression (for example, `last_access_time <= 3 Years AND file_size >= 5GiB`).

. Optionally, select an *Action* and choose the notification type (for example, *Email update*).

. Click *Save*.

.Result
The policy is saved and appears on the link:manage-policies.html[Policies] page. If you associated an action, AI Data Engine applies that action to files that match the policy criteria.

== Example: identify cold archive files

The following example creates a policy to tag files that have not been accessed in over a year.

. In Investigation, apply these filters:
+
* *Last Accessed Time* > 365 days

. Click *+ Create policy*.
. Enter the policy name: *Cold Files — Unaccessed 1 Year*.
. Select the action *Tag* and specify the tag value (for example, *cold-candidate*).
. Click *Save*.

== Related information

* link:manage-policies.html[Manage policies]
* link:take-actions.html[Take actions on data]
* link:../investigate/investigate-data.html[Investigate your data]
